.searchbar
    .container 
        form(action="/search", class = "search__form", role="search ", method="POST")
            input(type="search", aria-label = "Search", id = "searchInput", name="searchTerm", placeholder = "Search for event")
        div(id="searchClose") Close